# StarForge 项目标记：`sf` 命令以此发现项目根，勿删除。
# 本仓库是官方示例项目（starforge-tutorial），不是 CLI 工具仓。
# 日常微调请：pip install starforge-cli && sf init <你的项目>
apiVersion: forge/project/v1
name: starforge-tutorial
created_by: starforge-cli 0.1.0
