# SFT 实验：qwen3.5-4b 在 Alpaca 指令数据上做监督微调（补齐 SFT/GRPO/Agent 三种方法）。
# 继承官方 sft.yaml 基底 + qwen3.5-4b 片段，只写差异。官方入口 examples/run_sft.py
# （sft recipe 已固定 SFT 入口）。
defaults:
  - ../../configs/base/sft.yaml
  - ../../configs/models/qwen3.5-4b.yaml

# ══════════════════════════════════════════════════════════════════════════
#  调参速查（SFT）—— 你主要动【① 调参区】这几行。硬件/分布式由服务端 profile 注册表下发，不在这。
#  本实验目标集群：提交时 sf submit --profile 指定（推荐值见 README）；下列数值按该卡显存调，换卡 sf submit --profile 覆盖（通常要重调）。
# ──────────────────────────────────────────────────────────────────────────
#  lr (policy.optimizer.kwargs)           学习率。SFT 全参 ~1e-5/5e-6。太高过拟合/不稳，太低学不动
#  max_num_epochs / max_num_steps (sft)   训几轮 / 步数上限（先到先停）
#  val_period (sft)                       多少步验证一次
#  max_total_sequence_length (policy)     上下文长度。大→更吃显存、更慢
# ══════════════════════════════════════════════════════════════════════════

# ───────────────────────────── ① 调参区（按需改）─────────────────────────────
policy:
  max_total_sequence_length: 2048
  # 基底 sft.yaml 设了 squad 专用 chat_template；指令数据用模型默认模板，置 null
  tokenizer:
    chat_template: null
  optimizer:
    kwargs:
      lr: 5.0e-6

sft:
  max_num_epochs: 3
  max_num_steps: 2000
  val_period: 100

# ────────────────────────── ② 一般不动（数据接法 / 日志）──────────────────────────
# 用 ResponseDataset 接预处理好的 Alpaca jsonl（见 common/data/prepare_alpaca.py）。
# 先跑预处理脚本，再 `export ALPACA_DATA_DIR=<repo>/datasets/alpaca`。
data:
  _override_: true
  max_input_seq_length: ${policy.max_total_sequence_length}
  add_bos: true
  add_eos: true
  add_generation_prompt: false
  shuffle: true
  num_workers: 1
  train:
    data_path: ${oc.env:ALPACA_DATA_DIR}/train.jsonl
    input_key: input
    output_key: output
  validation:
    data_path: ${oc.env:ALPACA_DATA_DIR}/val.jsonl
    input_key: input
    output_key: output
  default:
    dataset_name: ResponseDataset
    input_key: input
    output_key: output
    prompt_file: null
    system_prompt_file: null
    processor: "sft_processor"

logger:
  wandb_enabled: false
  tensorboard_enabled: false
  swanlab_enabled: false
  monitor_gpus: true
